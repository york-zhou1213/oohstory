# 2026-04-02

- 修复 `webnovel-writer/frontend/src/views/CharacterView.vue`：新建档案弹窗的分类下拉框此前漏掉 `怪物库`，已补上创建入口。
- 已在 `/root/.openclaw-tcs/workspace/webnovel-projects/原初序列/设定集/怪物库/` 初步落盘 4 个怪物卡文件：`灰背短牙兽.md`、`黑背拱鼻兽.md`、`短鬃野猪.md`、`硬鬃獠牙猪.md`。其中部分数值为根据正文估算补位，若后续继续扩写怪物库必须再次逐章核对，不能把估算值当成已实锤设定。
- 势力库等级口径已按新《力量等级体系.md` 做过两轮同步：
  1. 先把旧 `Ω·相位/共振/超弦` 及 `前期/后期` 写法统一改为 `Ω·终焉三律之相位/共振/超弦`，并把对应代表人等级抬入新 Ω 区间；内层仓库提交：`f02841b` / `同步势力库代表人等级与位阶新口径`。
  2. 再把势力库中 `SS级` 与 `SSS级` 代表人的总等级映射修正到新体系区间（SS: 156-200，SSS: 201-260），保留人物内部强弱顺序；内层仓库提交：`e2e779e` / `修正势力库SS与SSS代表人等级区间`。
- 新规则记忆：以后若用户要求“按新力量等级体系同步势力库”，不能只改位阶名，必须同时检查并修正代表人的总等级数值，至少要覆盖 Ω、SSS、SS 三层，否则会出现位阶与等级区间不闭环。
