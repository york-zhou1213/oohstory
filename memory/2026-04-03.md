# 2026-04-03

- 角色管理“怪物库创建后前端没有显示”的根因已确认并修复：前端 `frontend/src/views/CharacterView.vue` 的 `tabs` / `creatableTabs` / 新建分类选项其实已经包含 `怪物库`，真正缺的是后端 `webnovel-writer/backend/routers/characters.py` 的 `list_characters()` 返回值。已在该函数的 `result` 初始化中加入 `"怪物库": []`，并在 `lib_mapping` 中加入 `"怪物库": ["怪物库"]`，这样 `/api/characters/` 返回的 `categories` 才会带上怪物库列表。
- 同一轮怪物库修复里，还补齐并确认了 `webnovel-writer/backend/routers/characters.py` 的创建链路：`_ensure_world_files()`、`create_character()` 的 `category_dirs`、以及 `monster_template` 本体都必须包含 `怪物库`，否则会分别出现 `创建失败: 无效的分类` 或 `Request failed with status code 500` / `NameError: monster_template is not defined`。
- 处理怪物库修复时，曾因 8080 端口旧 `uvicorn` 进程未退出导致新代码未生效；最终做法是停掉旧进程后再从 `/root/.openclaw-tcs/workspace/webnovel-writer/backend` 启动 `main:app`，验证 `http://127.0.0.1:8080/api/characters/` 已返回 `怪物库` 分类及现有文件列表。
- 今日已在 `/root/.openclaw-tcs/workspace/webnovel-projects/原初序列/设定集/怪物库/` 新增并落盘两张怪物卡：`草原狼.md`、`成年青鬃野猪.md`。其中草原狼用户明确给出完整掉落口径；成年青鬃野猪目前只有基础数值与经验，常见掉落/概率掉落仍是 `待补充`，后续若补全必须以用户或正文资料为准，不能脑补。
- 次要角色推进节奏继续维持“每批 3 个、先核势力库锚点、资料不够不写满”的规则。今日已完成并提交两批：
  - 第六批：`多拉 / 奥菲莉雅 / 科妮莉亚`，内层仓库提交 `2eeaafe` / `完善第六批次要角色`
  - 第七批：`多琳 / 阿斯特丽德 / 赛琳娜`，内层仓库提交 `0a19f13` / `完善第七批次要角色`
- 今日这两批次要角色继续严格执行既有创作红线：职业必须写成个人伟力成长路径，不能把 `院长 / 首席法官 / 管理总长 / 救援队负责人` 之类身份职责直接写成职业；技能必须保持网游技能树写法；等级、位阶与职业总等级构成继续闭环。
- 今日已在 `webnovel-writer` 中完成 Codex 浏览器授权接入的第一轮闭环：先全局安装 `@openai/codex` CLI，确认 `codex login status` 可用；随后在 `backend/routers/ai.py` 新增 `/api/ai/codex/status`、`/api/ai/codex/login/start`、`/api/ai/codex/logout`，并在 `frontend/src/api/index.js` 与 `frontend/src/views/ProjectView.vue` 接入“连接 Codex / 刷新状态 / 退出 Codex”按钮与状态展示。相关独立仓库提交：`0648e43` / `增加 Codex 浏览器授权入口与状态管理`。
- 同日又修正了“浏览器不自动打开授权页”的链路：后端从 `codex login` 日志中提取 OpenAI OAuth 授权链接（`auth_url`），前端点击“连接 Codex”后改为由当前用户浏览器主动 `window.open(auth_url)` 打开，而不是指望服务器进程弹桌面浏览器；并在页面上额外显示“打开 OpenAI 授权页”备用链接。相关提交：`8e7ab86` / `改为前端主动打开 Codex 授权页`。
- 关键新规则（Codex 远程授权场景）：如果用户是在**远程网页**里访问项目管理后台，而不是在服务器本机浏览器打开页面，则 `codex login` 默认给出的 `redirect_uri=http://localhost:1455/auth/callback` 会把回调打到**用户自己电脑**的 localhost，而不是服务器，因而浏览器侧出现 `ERR_CONNECTION_REFUSED` 属于链路天然不通，不是用户操作错误。
- 对上述远程授权问题，当前可行补救方式已验证成功：当用户完成 OpenAI 登录后，把最终的 `http://localhost:1455/auth/callback?...` 完整 URL 发回；服务器侧可将该 callback 代转发到本机 `127.0.0.1:1455`，从而让运行中的 `codex login` 进程完成登录。此次已实际验证成功，服务器当前状态为 `Logged in using ChatGPT`，且 `/root/.codex/auth.json` 已生成，`/root/.webnovel/codex/login.log` 记录了 `Successfully logged in`。
- 后续若继续完善 Codex 接入，优先方向不是再赌自动 localhost 回调，而是面向远程使用场景补一套正式闭环：优先候选为（1）前端增加“手动粘贴 callback URL”输入框 + 后端转发到 `127.0.0.1:1455`；或（2）研究切换到 `codex login --device-auth` 以规避 localhost 回调限制。
