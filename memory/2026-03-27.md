## 2026-03-27
- 已将 `/root/.openclaw-tcs/workspace/webnovel-writer/frontend/src/views/WorldMapView.vue` 整页重写为干净版本，并重新 `npm run build` + 重启 `webnovel-frontend`；当前链路主目标是打通“世界格局区域/势力下点角色 -> 右侧显示角色详情 -> 点击‘打开角色卡’ -> 跳转 `/workspace/characters` 并依赖 `route.query.open` / `category` 自动打开对应角色”。
- 用户当前处于手机使用场景，明确提出“帮我做手机兼容显示，我现在没法用电脑”；后续前端调整优先级应偏向移动端可点、可看、可编辑，而不是桌面端视觉优化。
- 用户对 `write.superforward.org` 的“白名单”理解为 IP 白名单；需记住此前在 `/root/.openclaw-tcs/workspace/webnovel-writer/frontend/vite.config.js` 中配置的 `server.allowedHosts: ['write.superforward.org']` 只是 Vite 的 Host/域名白名单，不限制来源 IP。真正的 IP 白名单需要在 nginx（如 `/etc/nginx/sites-available/write.superforward.org`）用 `allow` / `deny` 等规则实现。
