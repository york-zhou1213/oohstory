- 继续推进 webnovel-writer 的“作品基调 tone_tags”全链路：创建项目页已做标签式多选，项目管理页补了相同多选、推荐逻辑与保存；后续又补到自动预选推荐 + 持久化，初始化优先读取项目 state 中的 tone_tags。
- 推荐逻辑已从按题材推荐，细化到“题材 + 子风格”联合推荐，并在项目管理页增加“当前作品基调”摘要展示。
- 用户反馈联动 bug：切换子风格时作品基调不变化。已定位为只在默认/空值时才自动推荐的条件过粗，后续修正方向是增加“是否手动改过基调”的标记（toneTagsTouched），未手动改过时应随题材/子风格自动重算。
- 用户新增需求：首页项目显示卡片支持重命名。已完成前后端支持：后端新增项目重命名接口，首页卡片接入 InputDialog 弹窗重命名，并持久化项目标题。

- 2026-04-11: 修复 webnovel-writer B 类项目初始化相关问题。前端 `ProjectView.vue` 调整为在 `ai_init_progress.finished === true && success === false` 时，B/C 模式仍显示初始化自定义卡片；后端 `backend/routers/projects.py` 修正旧失败项目状态回填，明确返回“初始化失败但可恢复”。
- 2026-04-11: 修复 B 类金手指链路。为项目状态持久化补齐 `golden_finger_name`、`golden_finger_type`、`golden_finger_mode`；放宽 B 类金手指设计生成触发条件，空模板也可生成；并修复 `skill_executor.py` 中 `golden_finger_mode` 未透传到 `_ai_fill_init_content_stream(...)` 导致的 `name 'golden_finger_mode' is not defined`。
- 2026-04-11: 为 `backend/services/genre_catalog.py` 中科幻题材新增约束，禁止默认以 AI/人工智能 作为未来题材核心母题或主驱动力，优先引导到航天、机甲、星际殖民、基因工程、仿生改造、能源革命、文明接触等方向，除非用户明确指定要写 AI 主题科幻。
- 排查 OpenClaw `openai-codex` OAuth 失败时，确认当前默认模型配置是 `openai-codex/gpt-5.4`，文档明确该路由走 ChatGPT/Codex OAuth，不是 OpenAI Platform API key。
- 定位到失败根因不是用户 model 配置错误，而是 OpenClaw 2026.4.10 的 `openai-codex` OAuth 实现硬编码请求 scope：`openid profile email offline_access model.request api.responses.write`；其中 `model.request` 被当前 OAuth client 拒绝，导致回调为 `error=invalid_scope`，随后本地页面显示 `Authentication failed, missing authentication code.`
- 关键定位文件：`/usr/lib/node_modules/openclaw/dist/provider-auth-login.runtime-NTcFQaBC.js`；相关文档：`/usr/lib/node_modules/openclaw/docs/providers/openai.md`、`/usr/lib/node_modules/openclaw/docs/concepts/oauth.md`。
