## 2026-03-23
- 修复 webnovel-writer 公网访问 `write.superforward.org` 的前端 Host 拦截问题：在 `/root/.openclaw-tcs/workspace/webnovel-writer/frontend/vite.config.js` 中为 Vite 增加 `server.allowedHosts: ['write.superforward.org']`。
- 确认公网访问失败的后续根因不是 nginx 未承接，而是前端 API 原先写死为 `http://localhost:8080/api`，导致公网浏览器把 localhost 指向访问者本机；已改为相对路径 `/api`。
- 为兼容“本地直连 Vite”和“公网经 nginx 反代”两种模式，在 `vite.config.js` 中补充 Vite dev proxy：`/api -> http://127.0.0.1:8080`。
- 在 `/etc/nginx/sites-available/write.superforward.org` 中增加 `/api/` 反代到 `127.0.0.1:8080/api/`，其余 `/` 继续反代到前端 `127.0.0.1:5173`。
- 已验证两种调用都正常：`http://127.0.0.1:5173/api/projects/list` 返回 200，`https://write.superforward.org/api/projects/list` 也返回 200。
- 用户明确需求：希望系统可自由选择“仅本地访问”或“开启 nginx 反向代理公网访问”；当前配置已同时支持这两种模式，无需再改代码即可切换使用方式。
