- 2026-03-25：已修复 webnovel-writer 的“世界观管理”前端与后端联动问题。前端 `frontend/src/views/CharacterView.vue` 支持在网页内直接创建 `宝物库 / 功法库 / 势力库 / 地点库`，且已升级为表单化创建；创建后会自动生成对应 Markdown 并打开编辑。
- 2026-03-25：后端 `backend/routers/characters.py` 已补齐通用世界观档案模板与自动初始化逻辑；`/api/characters/create` 现支持 `主要角色 / 次要角色 / 反派角色 / 宝物库 / 功法库 / 势力库 / 地点库`，模板改为跨项目通用字段，不再只偏某个题材项目。
- 2026-03-25：处理过 `frontend/src/views/CharacterView.vue` 末尾重复残片导致的 Vite 报错 `[plugin:vite:vue] Invalid end tag`，并验证 `npm run build` 与 `webnovel-frontend` 重启通过。
