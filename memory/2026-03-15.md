
## Durable notes from 2026-03-15
- Bingo explicitly wants proactive closure on installs/long-running tasks: after any install or background task completes, notify them immediately without waiting for them to ask.
- Bingo keeps OpenClaw Browser Relay enabled by default; for browser tasks, do not ask again whether relay is on unless there is concrete evidence it is disconnected.
- Current desktop repair workflow: GNOME/GDM on this VMware-like virtual GPU environment produced black screen issues in console access. Switched GDM to Xorg, but black screen persisted with vmware/vmwgfx errors like "No surface to present from." Then started migration attempt to lightdm + xfce as a more compatible fallback.
- Fcitx5 was installed and configured successfully for the openclaw desktop user with Chinese input, left-Shift toggle behavior, Windows-like defaults, and autostart.
- For Pritunl at vps3.superforward.org, browser relay control is unstable across page transitions. Manual desktop-control fallback worked better: clipboard extraction from the visible page yielded FIN organization users partially. Extracted FIN names so far: Christy, D, Ercules, Hebe, Noe, Sarrah, ShanLong, amber, biubiu-vps3, ercules-vps3. Page indicated FIN has 12 users, so 2 remained unresolved.
- Reusable browser extraction fallback learned from the Pritunl FIN task: when OpenClaw Browser Relay can log in and click initial navigation but loses control after page transitions (`tab not found`), use the local desktop session directly instead of giving up. Practical sequence that worked:
  1. Verify Chrome window exists with `wmctrl -lx`.
  2. Focus the target Chrome window via `wmctrl -ia <windowid>`.
  3. Capture desktop screenshots with `import -window root ...` when relay snapshots are unreliable.
  4. Use `tesseract` OCR on cropped screenshots to identify visible page state and button labels (this located the `Next Page` button).
  5. Install/use `xdotool` + `xclip` to interact with the live desktop and extract visible page text via `Ctrl+A`, `Ctrl+C`, and clipboard reads.
  6. For paginated tables, combine OCR-based button detection with desktop mouse clicks to reveal additional entries.
- Specific result from that workflow: FIN in Pritunl had 12 users; final full list extracted was Christy, D, Ercules, Hebe, Noe, Sarrah, ShanLong, amber, biubiu-vps3, ercules-vps3, kyra-vps3, mikoy-vps3.
- Bingo wants a daily expense bookkeeping workspace in PHP (Philippine Peso). Created `/root/.openclaw-tcs/workspace/finance/` with monthly CSV ledger files in `finance/expenses/` and monthly summaries in `finance/reports/`. Future expense messages should be appended and summarized by day, by month, and by category.
