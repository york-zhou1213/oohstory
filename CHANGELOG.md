# Changelog

## web-v20260815-v15-28-oss1（2026-08-15）

版本号：`web-v20260815-v15-28-oss1`

- 新增 7 天拆书任务市场：用户可发布、认领、释放并交付拆书任务，逾期任务会安全回到可认领状态；
- 积分钱包升级为百分之一积分的整数账本，支持小数余额、按阅读时长兑换（1 小时 = 1 积分）及并发安全入账；
- 拆文完成审核并正式入库后，按原文非空白可见字符数除以 30 万一次性发放贡献奖励，同一投稿幂等防重；
- 拆文贡献者可在已入库作品中选择免费下载，或设置 0.01–999 积分售价；旧价购买返回 409，已购下载权永久保留；
- 「投稿」与「我的投稿」职责分离：前者集中上传与审核历史，后者只展示正式入库作品、详细档案、价格和收益；
- 已入库书籍与拆文支持书库封面、默认封面及贡献者自定义封面，上传图片会安全解码、去元数据并统一格式；
- 「投稿」重构为响应式创作者工作台：统一类型切换、小说三步上传、紧凑档案筛选，并覆盖深色和减少动效模式；
- 认证接口不再消耗通用瞬时 IP 限流桶，避免普通阅读流量导致有效登录或会话探测被误判为 429；
- 扩充用户投稿、版权与下架说明；公开版本继续使用示例域名和脱敏联络方式。

公开仓库仅同步程序、测试、品牌资源和示例配置，不包含小说正文、用户数据、数据库、音频缓存、生产域名/IP、站点验证文件、签名产物、备份或凭据。

## web-v20260815-v15-27-oss1（2026-08-15）

版本号：`web-v20260815-v15-27-oss1`

- 修复移动端媒体栈重复请求同一个长音频 URL 时，从旧 `start` 参数重新播放已听内容的问题；
- 播放进度现在绑定 session、manifest 与 stream ID，并记录已确认片段及段内毫秒位置；
- 重连请求从最新确认游标继续，响应公开请求起点、实际续播点与 `X-Audiobook-Replay-Resumed` 状态；
- 连续音频按真实 MP3 时长节奏发送，限制预缓冲，避免生成游标跑到实际播放位置之前；
- 增加跨 Worker 游标锁、客户端进度优先窗口、章末 100ms 安全续接及过期锁清理；
- GitHub Actions 改为按测试模块隔离 Reader/Admin 状态，并固定 Flutter 3.32.4 工具链；
- 公开仓库补齐自包含的 Reader 评论表迁移、公共 Reader 最小权限账号，并修复测试对私有同级仓库和持久限流状态的隐式依赖。
- Admin 容器、宿主机部署文档与 CI 补齐 ImageMagick 运行依赖，保证占位封面视觉分类在全新环境可用。

## web-v20260814-v15-26-oss1（2026-08-14）

版本号：`web-v20260814-v15-26-oss1`

本次同步把 Reader、Admin/书库 Worker 与 Flutter 客户端更新到同一公开基线，主要包括：

- 智能多角色听书：引号意图、人物/性别证据、角色声线快照、精确时间轴和可续传章节音频；
- Web 与 Android 后台连续播放、从正文坐标起播、章节切换和本地音频缓存；
- 本地密码与可选 Google 登录、通知、投稿记录及账户界面更新；
- 段落评论、对象存储隔离、维护模式、错误边界和上传 Worker；
- 每 IP 每日下载额度、章节遍历风控、签名蜜罐及 Cloudflare 真实 IP 部署模板；
- Admin 维护入口、书库任务状态、串行更新 Worker、EPUB 文本安全处理及覆盖图/索引任务更新；
- Flutter `1.18.20+64` 源码与更新检查合同。

公开仓库只包含程序、测试、品牌资源和示例配置。小说正文、用户数据、数据库、音频缓存、生产域名/IP、站点验证文件、签名 APK 与任何凭据均不在同步范围内。
