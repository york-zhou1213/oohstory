# Security Policy

## 支持范围

安全修复以当前 `main` 分支为准。请不要在公开 Issue 中披露可直接利用的细节或任何真实凭据。

## 报告漏洞

请使用 GitHub Security Advisories 的私密报告功能。报告应包含：

- 受影响版本与组件
- 最小复现步骤
- 影响范围与建议缓解方案
- 是否可能涉及真实用户数据

请不要附上真实密码、Cookie、OAuth Token、私钥或生产数据库导出。

## 安全边界

- 应用默认只监听 `127.0.0.1`，公网流量由受限 Nginx 反向代理进入。
- 账户写操作使用 CSRF、SameSite/HttpOnly Cookie、请求体上限与分区限流。
- 书籍、封面、下载和投稿路径在解析后必须位于明确的数据根目录内。
- 投稿在隔离目录中扫描与审核，不允许直接写入公开书库。
- 密码、SMTP 凭据和第三方令牌必须通过环境或权限受控文件提供，不得进入 Git。
- 管理后台不属于公网 Reader 路由；示例 Nginx 对 `/admin` 和 `/api/v1/admin/` 显式返回 404。

## 发布前自检

1. 扫描 `.env`、数据库、备份、APK/ZIP、证书和高熵令牌。
2. 确认部署模板中只有 `reader.example.com` 等示例值。
3. 检查 Git 历史，而不只是当前工作树。
4. 使用最小权限系统用户和数据库账号。
5. 在公网上验证未知写操作、后台路由和路径穿越请求均失败关闭。
